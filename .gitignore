.DS_Store

# Context / roadmap docs - not for public repo
Context/

# Private working log - full correction/decision history kept locally for the
# author's own reference; a headline-only summary lives in README's Corrections
# section instead.
gate_decisions.md

# Large re-downloadable inputs. Every one of these is a public accession and is
# listed with its source in the README, so the repo stays clonable without them.
data/raw/c_elegans.PRJNA13758.WS285.annotations.gff3.gz
ref_data/go/go-basic.obo

# Platform-specific binary, not ours to redistribute
data/liftover/liftOver

# Python bytecode cache, created by importing scripts/figure_style.py as a module
scripts/__pycache__/
